export const procesoPesado = (iteraciones) => {

    for (let index = 0; index < iteraciones; index++) {
         
    }
    console.log("Se ejetcutaron "+iteraciones+" procesos.");
    return ("Se ejetcutaron "+iteraciones+" procesos.");
}